---
scope: Litmus v1 — personal week-planner + projects web app (whole product, greenfield, no code yet)
purpose: build-substrate
altitude: feature (keeps epics coherent)
updated: 2026-08-19T13:23
---

- (event) Inputs read: docs/prd.md (final, 37 FRs / 7 feature groups / 5 UJs), docs/prd-addendum.md (21 design tokens, frame traceability, deferred Notes+Link Note Modal specs), PRD .memlog.md, AGENTS.md project context. No project-context.md exists; no application code exists — greenfield.
- (constraint) [INHERITED from PRD] v1 is single-user, single Account. No collaboration, permissions, roles, sharing, invitations. Auth exists ONLY for cross-device data portability and must not become the foundation of a permission model.
- (constraint) [INHERITED from PRD] Synced from day one: backend + single account + auth. Offline read+edit tolerated; changes queue and sync on reconnect. Sync conflicts resolve last-write-wins PER FIELD.
- (constraint) [INHERITED from PRD] Optimistic local update is mandatory: drag-and-drop, status change, subtask toggle must reflect immediately, never awaiting a round trip. Week Board full-week warm load under 1s.
- (constraint) [INHERITED from PRD] All colour resolves through 21 named design tokens with paired light/dark values; no hard-coded colour outside the token layer. WCAG AA in both themes.
- (constraint) [INHERITED from PRD] Responsive 390px to 1600px, no horizontal page scroll. Every drag action has a non-drag equivalent.
- (constraint) [INHERITED from PRD] Privacy: single-tenant personal data, no third-party analytics/telemetry/embeds on any screen. Passwords salted-hash only; session tokens not readable by page scripts.
- (constraint) [INHERITED from PRD FR-35/36/37] Cross-surface invariants: Due Date is the sole mechanism placing a Task on the Week Board; Status is one shared three-value enum; Progress is always derived (Done/total), never stored as entered.
- (constraint) [INHERITED from PRD] MVP excludes the entire Notes surface (v2), note linking, command palette, notifications, integrations, import/export, AI, recurring tasks.
- (direction) Altitude set to feature-level: the spine governs Litmus v1 as a whole and must keep its epics from diverging. Downstream is bmad-create-epics-and-stories.
- (event) Run folder bound; spine seeded from template.
- (decision) Working mode = Coaching path. User works decisions with me rather than reviewing a fast draft.
- (decision) Deliverables = ARCHITECTURE-SPINE.md plus a human-readable walkthrough rendering (form chosen at finalize). Audience is the builder himself, six-months-later.
- (decision) OFFLINE EDITING CUT from v1. User confirmed always-online is acceptable; server is authoritative, optimistic UI for snappiness, honest error when the network is gone. OVERRIDES PRD FR-8 bullet 2 and the 'Offline tolerance' cross-cutting NFR. Must offer to amend docs/prd.md at finalize.
- (decision) Consequence of cutting offline: no local-first sync engine. Zero 1.0 (Rocicorp, stable June 2026), Livestore, ElectricSQL and Triplit were the live candidates and are all rejected as unjustified machinery for a single human editor. Per-field LWW (PRD assumption 6) becomes moot — no client-side conflict resolution layer is built.
- (decision) UI language/ecosystem = React. User's existing familiarity; also the most mature ecosystem for the drag-and-drop and board interactions this product needs.
- (version) Web-verified 2026-08-19: Next.js 16 is LTS, latest 16.3.0 (2026-08-03); React Compiler support stable in 16. TanStack Start reached stable 1.x in early 2026.
- (version) Web-verified 2026-08-19: Auth.js/NextAuth is in maintenance mode under Better Auth's stewardship as of early 2026; Better Auth is the recommended path for new self-hosted email+password projects.
- (version) Web-verified 2026-08-19: Drizzle ORM is the default community recommendation for new TypeScript+Postgres projects in 2026; Prisma 7 (Nov 2025) replaced its Rust engine with TS/WASM. Both viable.
- (version) Web-verified 2026-08-19: @dnd-kit is actively maintained and the default React drag-and-drop choice (used by Linear, Vercel dashboard); Atlassian Pragmatic DnD is the alternative, only advantaged at Jira/Trello scale. Litmus boards are 50-200 items at most.
- (direction) USER-SUPPLIED STACK PLAN supersedes prior exploration. Earlier Next.js/Better Auth/Drizzle/Postgres research is void; retained only as rejected-alternative context.
- (decision) [ADOPTED] Frontend = React + Vite + TypeScript + TanStack Query. Styling = CSS Modules. User-specified.
- (decision) [ADOPTED] Backend = C# (.NET) on AWS Lambda. API Gateway fronts it as a proxy to a SINGLE Lambda function (not function-per-route). User-specified.
- (decision) [ADOPTED] Auth = AWS Cognito. Replaces the Better Auth line entirely. User-specified.
- (decision) [ADOPTED] Database = DynamoDB. S3 added later for Notes markdown blobs (v2). User-specified.
- (decision) [ADOPTED] Infrastructure as code = AWS CDK. Three environments: dev, staging, production. User-specified.
- (decision) [ADOPTED] CI/CD = GitHub Actions. Branch-to-environment mapping: develop=dev, qa=staging, main=production. Merge develop->qa triggers a staging release; merge qa->main triggers a production release. User-specified.
- (constraint) [ADOPTED] Keeping running cost at approximately zero is a stated priority, across all three environments.
- (decision) [ADOPTED] Both frontend and backend follow feature-based modular development. Core modules at v1: weeks, projects. Notes added later as a third core module. User-specified.
- (question) OPEN: source-code layout. User leans Nx for the frontend but is unsure whether to bring the C# backend into the Nx workspace, citing immature tooling. Needs a decision.
- (version) Web-verified 2026-08-19 (cost): Lambda free tier 1M req + 400k GB-s per month, always free, no expiry. CloudFront always-free 1TB egress + 10M requests/mo, no expiry. DynamoDB 25GB storage always free. Cognito 10,000 MAU free per account/organization indefinitely on Lite or Essentials tiers.
- (version) Web-verified 2026-08-19 (cost traps): API Gateway 1M req/mo free is 12 MONTHS ONLY; after that HTTP API is $1.00/M and REST API $3.50/M. DynamoDB ON-DEMAND is billed from the first request with ZERO free-tier offset (only the 25 GB storage applies); the always-free 25 WCU/25 RCU applies to PROVISIONED mode and is account-wide across all tables. Route53 public hosted zone is $0.50/month each - the one guaranteed non-zero line item.
- (version) Web-verified 2026-08-19: SnapStart is FREE only for Java managed runtimes. For .NET it charges for snapshot caching (billed from a 3-hour minimum, then per-ms, scaled by function memory) plus ~$0.00014 per restore. Caching accrues 24/7 per published function version, so 3 environments x SnapStart is a continuous charge.
- (version) Web-verified 2026-08-19: .NET 10 is available as an AWS Lambda managed runtime and base image. Native AOT + ARM64/Graviton gives the best cold-start (arm64 init 13-24% faster than x86); Native AOT must be BUILT on an ARM64 machine to target Graviton.
- (version) Web-verified 2026-08-19: @nx-dotnet/core is NO LONGER MAINTAINED. The official replacement @nx/dotnet exists only in Nx 22+ and is explicitly EXPERIMENTAL, requiring .NET SDK 8.0+. This confirms the user's instinct that .NET tooling in Nx is not mature.
- (version) Web-verified 2026-08-19: aws-cdk-lib is on the 2.260.x line as of mid-2026, weekly releases, Node.js 22.x required baseline.
- (question) CONFLICT surfaced: PRD NFR 'Session tokens are not readable by page scripts' vs Cognito in a SPA. Cognito's browser SDKs store tokens in localStorage, which page scripts CAN read. Resolving it requires either relaxing the NFR or adding a token-exchange endpoint on the Lambda that sets httpOnly cookies (BFF pattern). Escalate to user.
- (question) CONFLICT surfaced: the PRD's three designed auth screens (TsKgU/Y9MTL5/xZjeU) specify an exact two-pane layout, headline copy and decorative Task Card instances. Cognito Hosted UI / Managed Login cannot render that. Custom screens calling Cognito auth APIs directly are required if the designs are honoured. Escalate to user.
- (question) OPEN: cold-start posture for the single C# Lambda. Plain .NET 10 managed runtime (free, ~1-2s cold) vs SnapStart (costs money continuously, x3 envs) vs Native AOT+ARM64 (free and fastest, but forbids reflection - forces source-generated JSON and the low-level DynamoDB Document/AttributeValue API instead of DynamoDBContext, which constrains every module's data access).
- (constraint) COST INVARIANT identified: the Lambda must NOT be placed in a VPC. A NAT Gateway is ~$32/month each and would single-handedly destroy the ~$0 target. DynamoDB, Cognito and S3 are all reachable over public AWS endpoints with IAM auth, so no VPC is needed.
- (constraint) COST INVARIANT identified: CloudWatch log groups default to never-expire retention; storage accrues indefinitely. Retention must be set explicitly on every log group in CDK.
- (direction) STRUCTURAL RISK identified: user named 'weeks' and 'projects' as the core modules, but Task is read and mutated by BOTH. If either module owns Task, the surfaces drift - which is precisely the failure PRD 4.7 (FR-35/36/37) warns about. Proposed resolution: a shared domain core owning Task/Project/Status/Category and all derivation rules, with weeks and projects as projection modules over it. Mirrored identically on frontend and backend.
- (direction) DATA MODEL direction: DynamoDB single-table, partition key = Cognito sub. The entire per-user dataset (design fixtures: 6 projects, 85 tasks) is a few hundred KB, so ONE Query on the user partition loads the whole working set and no GSI is needed at v1. Subtasks embed as an ordered list attribute on the task item rather than as separate items.
- (direction) DERIVED VALUES direction: Progress, task counts and 'due this week' counts are computed at read time from the task set and NEVER persisted. Persisted counters are the exact divergence hole FR-36 forbids - a status change on any path that forgets the counter update corrupts progress permanently. Needs an explicit scale tripwire.
- (decision) AUTH RESOLVED: custom-built auth screens honouring designs TsKgU/Y9MTL5/xZjeU, calling Cognito SRP auth APIs directly. Access/ID tokens held in JS memory only, never localStorage; refresh token persisted for 'Remember me'. Hosted UI and Managed Login rejected because they cannot render the designed two-pane layout. Cookie-BFF rejected as disproportionate for a single-user app. PARTIAL relaxation of the PRD NFR 'session tokens are not readable by page scripts' - the refresh token remains in persistent browser storage. Must be recorded as a known, accepted deviation.
- (decision) COLD START RESOLVED: plain .NET 10 managed runtime on ARM64/Graviton. No SnapStart (costs money continuously x3 envs), no Native AOT (its reflection ban would constrain every module's data access forever). ~1-2s first-request-of-day cold start accepted; the PRD's sub-1s target is explicitly a WARM load. Consequence: reflection is available, so DynamoDBContext and standard System.Text.Json are permitted.
- (decision) REPO LAYOUT RESOLVED: one Git repo. Nx governs TypeScript only (apps/web + libs/*), where its module-boundary lint rules enforce the feature-modular invariant. backend/ is a plain .NET solution built by the dotnet CLI. infra/ is CDK. GitHub Actions uses path filters so each side builds independently. Experimental @nx/dotnet deliberately not adopted.
- (decision) MODULE STRUCTURE RESOLVED: shared domain core + projection modules. A core module owns Task, Project, Status, Category and ALL derivation rules (Progress, counts, week membership). 'weeks' and 'projects' are projections that own surfaces, layouts and queries but never their own task logic. Notes joins later as a peer projection. Mirrored identically on frontend and backend. This makes FR-35/36/37 structurally unviolatable rather than merely documented.
- (decision) API SHAPE RESOLVED: hybrid. GET /workspace returns all projects plus a lean task index (id, title, status, dueDate, category, projectId, subtask counts). Task descriptions and subtask bodies load on demand when Task Detail opens. Keeps one client-side source of truth for both board surfaces while bounding the initial payload.
- (decision) ENV TOPOLOGY RESOLVED: ONE AWS account, three CDK stages with per-environment resource name prefixes (litmus-dev-*, litmus-qa-*, litmus-prod-*). User-specified. Mitigations required: termination protection on prod stacks, and a distinct deploy role.
- (decision) ROUTER RESOLVED: TanStack Router. Same family as TanStack Query, and typed search params carry the displayed week in the URL, which FR-10 requires to survive reload.
- (decision) FRONTEND HOSTING RESOLVED: all-AWS. S3 + CloudFront, owned by CDK, so a single IaC tool describes the entire system. Vercel evaluated and rejected: it was a tie on cost (custom domains are $0 on both) and its advantage was effort and preview deploys, which did not outweigh splitting the system across two IaC surfaces.
- (version) Web-verified 2026-08-19: ACM public certificates are free including auto-renewal; CloudFront alternate domain names (CNAMEs) carry no charge; ACM certs for CloudFront must be in us-east-1; since April 2025 CloudFront supports automated HTTP-validated public certificates. Route53 alias records to CloudFront incur no query charge. Route 53 is NOT required to front CloudFront - any DNS provider works via CNAME.
- (assumption) One Route 53 public hosted zone is adopted (~$0.50/month) rather than external DNS, because it lets CDK auto-validate ACM certificates and create alias records without a manual step - coherent with the all-AWS, CDK-owns-everything decision. This is the single known non-zero recurring line item. Three environments share the one zone as subdomains.
- (decision) PARADIGM: modular monolith with a shared domain core and surface projections. One deployable per side (one SPA bundle, one Lambda). Feature modules are projections over a core that owns the entities and every derivation rule; feature-to-feature dependencies are forbidden in both directions.
- (decision) Backend host = ASP.NET Core Minimal API with AddAWSLambdaHosting(LambdaEventSource.HttpApi), deployed as ONE Lambda behind an API Gateway HTTP API $default proxy route. Each backend module contributes routes through one endpoint-registration extension method. Runs locally as an ordinary Kestrel app, so the whole API is testable without deploying.
- (decision) API Gateway type pinned to HTTP API, never REST API. HTTP API is $1.00/M vs REST at $3.50/M once the 12-month free tier lapses, and JWT authorizers are native to HTTP API.
- (decision) The client generates entity IDs (UUIDv7) before sending a create. Prevents temporary-id reconciliation in optimistic inserts, and makes creates idempotent on retry. Server validates format and rejects an id that already exists.
- (decision) Due Date is a CALENDAR DAY stored as a YYYY-MM-DD string, never a UTC instant. Week Board membership is computed from that string against the user's local week. createdAt/updatedAt are separately ISO-8601 UTC instants. Prevents the timezone-shift bug where a task lands on the wrong day for a user east or west of UTC.
- (decision) API error responses use RFC 9457 Problem Details, which ASP.NET Core produces natively. One error shape across every endpoint.
- (decision) Authorization: API Gateway HTTP API JWT authorizer validates the Cognito access token before the Lambda runs. The Lambda reads the user's Cognito sub ONLY from the authorizer claims and never from a request body, path or query parameter. Every DynamoDB access is scoped to that sub.
- (decision) Cognito user pool tier pinned to Lite or Essentials, never Plus - Plus has no free tier at all and would break the ~$0 target.
- (decision) CI authenticates to AWS via GitHub OIDC role assumption. No long-lived AWS access keys in GitHub secrets, in any environment.
- (decision) Design tokens: the 21 named tokens are declared once as CSS custom properties on :root and [data-theme=dark]; CSS Modules consume only var(--token). A lint rule rejects raw colour literals in .module.css. Makes PRD FR-2 and the theming-integrity NFR mechanically enforced rather than reviewed.
- (decision) Client cache: ONE TanStack Query key factory lives in core. The workspace index is a single cache entry; every mutation optimistically patches that entry and all invalidation funnels through one function - which also reserves the seam for adding real-time push later without touching feature code.
- (direction) Honest asymmetry to record: there is NO backend 'weeks' module at v1. The Week Board is a pure client-side projection over the workspace index, so weeks exists only as a frontend feature module. This is a consequence of the hybrid API shape, not an oversight.
- (version) Live npm registry check 2026-08-19: vite 8.2.1, react 19.2.8, typescript 7.0.2 (latest tag), nx 23.1.1, @tanstack/react-query 5.101.4, @tanstack/react-router 1.170.30, aws-cdk-lib 2.265.0, @dnd-kit/core 6.3.1, eslint 10.8.1, typescript-eslint 8.67.0. Last stable TypeScript 6.x is 6.0.3.
- (decision) TypeScript pinned to 6.0.3, NOT the newer 7.0.2. Web-verified: TS 7.0 (native Go compiler) went GA 2026-07-08, but typescript-eslint closed its TS7 support request as 'not planned' on day one and ESLint core is blocked behind it. This spine's enforcement mechanisms - the Nx module-boundary rule and the raw-colour-literal rule - are BOTH ESLint rules, so adopting TS7 now would disable the very checks the invariants rely on. Revisit when 7.1 lands the stable API and typescript-eslint ships support.
- (decision) Backend runtime pinned to .NET 10 (LTS, supported to November 2028; AWS Lambda managed runtime since 2026-01-08). .NET 11 deliberately NOT adopted: it is STS with only two years of support, is still in preview, and does not ship until November 2026.
- (constraint) DURABILITY: the PRD's 'no user-entered content is lost' NFR is answered by DynamoDB point-in-time recovery, enabled on the production table. PITR bills on table size, which is a few MB here, so it stays inside the ~$0 envelope.
- (decision) Privacy NFR ('no third-party embeds on any screen') has a concrete structural consequence: the Inter typeface and Phosphor Icons must be SELF-HOSTED from the app's own origin. A Google Fonts link would be a third-party runtime call from every screen and would violate the NFR.
- (event) Reviewer Gate run. lint_spine.py: 7 low findings, all verified false positives - {sub}/{id}/{env} are intentional DynamoDB key and resource-naming syntax, not unfilled template tokens. Semantic lenses run SEQUENTIALLY rather than as parallel subagents, because the AgentTool is disabled in this session; full text in reviews/.
- (event) Gate lens 1 (rubric walker): strong on the domain risk and the operational envelope. Two gaps found - schema evolution silent, and an unowned ring of incidental shared state. Both fixed.
- (event) Gate lens 2 (currency): no decision rests on unverified recall. Caught two would-be defaults - TypeScript 7 and .NET 11 - and corrected one user-held premise about Route 53 costs. Gap noted: no OpenAPI-to-TS generator pinned (low risk, build-time dev dependency).
- (event) Gate lens 3 (adversarial seams): 10 divergence holes found where two compliant epics still build incompatibly - list ordering, optimistic-mutation races, project-delete cascade, updatedAt semantics, time-range typing, preference storage location, project colour form, item type discriminator, failure UX, category source. All closed.
- (decision) Gate fixes applied: AD-3 extended (derived updatedAt), AD-4 extended (time range as local HH:mm strings), AD-5 extended (fixed v1 category list, closes PRD Q7), AD-15 extended (colour stored as token name; automated contrast test). New AD-22 (deterministic derived ordering), AD-23 (per-entity mutation serialization, stale-response discard, single failure UX), AD-24 (preferences on the Account, view state in the URL), AD-25 (project delete cascades, projectId never dangles), AD-26 (type discriminator + schemaVersion on every item), AD-27 (one breakpoint scale, mobile-first, no horizontal scroll).
- (event) Input reconciliation: all 7 PRD feature groups plus persistence, API and delivery are present in the Capability to Architecture map. Two quiet cross-cutting NFRs initially dropped by the AD structure were recovered - the responsive 390-1600px envelope (now AD-27) and WCAG AA contrast verification (now in AD-15).
- (event) Spine finalized: status=final, 27 ADs, 4 diagrams. Versions pinned live from npm and NuGet registries on 2026-08-19.
- (event) Walkthrough rendering published as an Artifact: https://claude.ai/code/artifact/062785ac-d12b-4b02-bc8f-cb4add743e32 (source at walkthrough.html). Palette derived wholly from Litmus's own 21 design tokens so the document is themed by the product it describes; the six theme-invariant dot-* values code the six rule domains.
- (event) Run complete. Deliverables: ARCHITECTURE-SPINE.md (final, 27 ADs), walkthrough.html + published Artifact, reviews/ (3 lenses), .memlog.md.
